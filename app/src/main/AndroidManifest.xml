<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidcompiler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidCompiler">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Compilacion"/>
        <activity android:name=".Reportes.report_errores" />
        <activity android:name=".Reportes.report_animaciones" />
        <activity android:name=".Reportes.report_objetos" />
        <activity android:name=".Reportes.report_colores" />
        <activity android:name=".Reportes.report_operadores" />
    </application>

</manifest>